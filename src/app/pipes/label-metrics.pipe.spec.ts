import { LabelMetricsPipe } from './label-metrics.pipe';

describe('LabelMetricsPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelMetricsPipe();
    expect(pipe).toBeTruthy();
  });
});
